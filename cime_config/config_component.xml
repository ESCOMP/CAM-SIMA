<?xml version="1.0"?>

<entry_id version="3.0">

  <description modifier_mode="*">
  <!--
    ===============
      CAM
    ===============
   -->
    <desc atm="CAM70">CAM-SIMA with cam7 physics:</desc>
    <desc atm="CAM60">CAM-SIMA with cam6 physics:</desc>
    <desc atm="CAM50">CAM-SIMA with cam5 physics:</desc>
    <desc atm="CAM40">CAM-SIMA with cam4 physics:</desc>
    <desc atm="CAM[%ADIAB][%PHYSTEST][%DABIP04][%TJ16][%HS94][%KESSLER]">CAM-SIMA simplified and non-versioned physics:</desc>
    <desc atm="CAM%MPASCP">CAM-SIMA with MPAS-like "convection-permitting" physics suite (EXPERIMENTAL)</desc>

  <!--
    ===============
     SIMPLE MODELS Options
    ===============
   -->
    <desc option="ADIAB"        >CAM dry adiabatic configurarion (no physics forcing):</desc>
    <desc option="DABIP04"      >CAM dry adiabatic baroclinic instability (Polvani et al., 2004):</desc>
    <desc option="TJ16"         >CAM moist Held-Suarez forcing (Thatcher and Jablonowski, 2016):</desc>
    <desc option="HS94"         >CAM dry Held-Suarez forcing (Held and Suarez (1994)):</desc>
    <desc option="KESSLER"      >CAM moist dynamical core test with Ullrich et al. (2014) baroclinic wave IC, Kessler physics:</desc>

  <!--
    ===============
     Physics Testbed
    ===============
  -->
    <desc option="PHYSTEST"     >CAM Physics Testbed (no dynamical core):</desc>

  </description>

  <entry id="COMP_ATM">
    <type>char</type>
    <valid_values>cam</valid_values>
    <default_value>cam</default_value>
    <group>case_comp</group>
    <file>env_case.xml</file>
    <desc>Name of atmospheric component</desc>
  </entry>

  <entry id="CAM_CPPDEFS">
    <type>char</type>
    <valid_values></valid_values>
    <default_value>UNSET</default_value>
    <group>build_component_cam</group>
    <file>env_build.xml</file>
    <desc>CAM cpp definitions (setup automatically - DO NOT EDIT)</desc>
  </entry>

  <entry id="CAM_DYCORE">
    <type>char</type>
    <valid_values>mpas,se,none</valid_values>
    <default_value>none</default_value>
    <values match="last">
      <value grid="a%mpasa[0-9]+">mpas</value>
      <value grid="a%ne[0-9]">se</value>
    </values>
    <group>build_component_cam</group>
    <file>env_build.xml</file>
    <desc>CAM dynamical core</desc>
  </entry>

  <entry id="CAM_CONFIG_OPTS">
    <type>char</type>
    <valid_values></valid_values>
    <default_value></default_value>
    <values match="last" modifier='additive'>
      <value compset="_CAM70">--physics-suites cam7</value>
      <value compset="_CAM40">--physics-suites cam4</value>
<!--  <value compset="_CAM50">-phys cam5</value>
      <value compset="_CAM60">-phys cam6</value> -->

<!--  <value compset="_CAM.*_BGC%B">-co2_cycle</value> -->

      <!-- Simple models -->
      <value compset="_CAM%ADIAB">--physics-suites adiabatic</value>
      <value compset="_CAM%TJ16">--physics-suites tj2016 --analytic-ic</value>
      <value compset="_CAM%KESSLER">--physics-suites kessler --analytic-ic</value>
      <value compset="_CAM%HS94">--physics-suites held_suarez_1994 --analytic-ic</value>
      <value compset="_CAM%PHYSTEST">--dyn none --physics-suites adiabatic</value>

      <!-- Aquaplanet -->
      <value compset="_DOCN%SOMAQP">--aquaplanet</value>
      <value compset="_DOCN%AQP"   >--aquaplanet</value>

      <!-- CAM with MPAS-like "convection-permitting" physics suite (EXPERIMENTAL) -->
      <!-- Temporarily using analytic initial condition during development -->
      <value compset="CAM%MPASCP">--physics-suites convection_permitting --analytic-ic</value>
    </values>
    <group>build_component_cam</group>
    <file>env_build.xml</file>
    <desc>
      CAM-SIMA configure options
      Provides option(s) for the CAM-SIMA configuration system.
      CAM_CONFIG_OPTS are normally set as compset variables (e.g., --physics-suites cam4 --dyn mpas)
      and in general should not be modified for supported compsets.  It is recommended that if you want
      to modify this value for your experiment, you should use your own user-defined
      component sets via using create_newcase with a compset_file argument
    </desc>
  </entry>

  <entry id="CAM_USER_MODS">
    <type>char</type>
    <valid_values></valid_values>
    <default_value></default_value>
    <values match="last">
      <value compset="DOCN%AQP">$COMP_ROOT_DIR_ATM/cime_config/usermods_dirs/aquap</value>
      <value compset="DOCN%SOMAQP">$COMP_ROOT_DIR_ATM/cime_config/usermods_dirs/aquap</value>
    </values>
    <group>run_component_cam</group>
    <file>env_case.xml</file>
    <desc>User mods to apply to specific compset matches. </desc>
  </entry>

  <entry id="CAM_SIM_YEAR">
    <type>char</type>
    <valid_values></valid_values>
    <default_value></default_value>
    <values match="last">
      <value compset="^1850_">1850</value>
      <value compset="^2000_">2000</value>
    </values>
    <group>run_component_cam</group>
    <file>env_run.xml</file>
    <desc>
      Simulation (climatology) year, set from the time period at the start of the compset name.
      Namelist defaults can be filtered by the sim_year XML attribute.
      A compset without a recognized time period is empty so no sim_year default applies.
    </desc>
  </entry>

  <entry id="CAM_LINKED_LIBS">
    <type>char</type>
    <valid_values></valid_values>
    <default_value> </default_value>
    <values match="last" modifier="additive">
      <!-- Add MPAS if using mpas grid and not using physics testbed -->
      <value compset="^((?!_CAM%PHYSTEST).)*$" grid="a%mpasa[0-9]+">-lmpas</value>
    </values>
    <group>build_component_cam</group>
    <file>env_build.xml</file>
    <desc>
      CAM-SIMA linked libraries. The libraries are built by
      CAM-SIMA's buildlib script and should be included
      during linking of the model executable.
    </desc>
  </entry>

  <help>
    =========================================
    CAM naming conventions
    =========================================
  </help>

</entry_id>
